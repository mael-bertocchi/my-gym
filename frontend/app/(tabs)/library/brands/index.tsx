import type { ReactElement } from 'react';
import { EmptyState, Screen } from '@/components';

/**
 * @function BrandsScreen
 * @description Stub for the Machine brands screen; body wired in a later task.
 *
 * @returns {ReactElement} The placeholder element.
 */
export default function BrandsScreen(): ReactElement {
    return <Screen><EmptyState message="Machine brands" /></Screen>;
}
