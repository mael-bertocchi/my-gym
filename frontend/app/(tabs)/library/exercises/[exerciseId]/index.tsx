import type { ReactElement } from 'react';
import { EmptyState, Screen } from '@/components';

/**
 * @function ExerciseDetailScreen
 * @description Stub for Exercise Detail; body wired in a later task.
 *
 * @returns {ReactElement} The placeholder element.
 */
export default function ExerciseDetailScreen(): ReactElement {
    return <Screen><EmptyState message="Exercise detail" /></Screen>;
}
