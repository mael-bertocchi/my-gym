import type { ReactElement } from 'react';
import { EmptyState, Screen } from '@/components';

/**
 * @function AddVariantScreen
 * @description Stub for the Add Variant sheet; body wired in a later task.
 *
 * @returns {ReactElement} The placeholder element.
 */
export default function AddVariantScreen(): ReactElement {
    return <Screen><EmptyState message="Add variant" /></Screen>;
}
